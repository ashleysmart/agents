# Feature Envy

A method that operates primarily on data from another class rather than its own.

**Applies to**: Object-Oriented Design

---

**What it is**: A method that operates primarily on data from another class
rather than its own.

**Why not**: The logic belongs in the class whose data it uses. Feature
envy signals a misplaced responsibility — move the method.
