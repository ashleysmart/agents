# Shotgun Surgery

A single logical change requires edits scattered across many unrelated files and classes.

**Applies to**: Software Design

---

**What it is**: A single logical change requires edits scattered across
many unrelated files and classes.

**Why not**: Signals the wrong cohesion boundaries. The concept that
changes together should live together (SRP at the module level).
