# Dependency Hell

A web of transitive dependencies with conflicting version requirements that cannot all be satisfied simultaneously.

**Applies to**: Dependency Management

---

**What it is**: A web of transitive dependencies with conflicting version
requirements that cannot all be satisfied simultaneously.

**Why not**: Builds break, upgrades become impossible, and security patches
cannot be applied. Keep dependency trees shallow and pin versions
explicitly.
